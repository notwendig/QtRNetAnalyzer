Place proprietary vendor SDK files here if you want to enable hardware integration.

Expected Linux path for the shared library:
- x86/64-linux/libcontrolcan.so

The repository intentionally does not include proprietary SDK files.
